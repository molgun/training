package com.example.demo.vo;

public class IdentityCheckService {

    public void check(IdentityNumber number) {
        String value = number.getValue();
        if (val) {
            
        }
    }
}
